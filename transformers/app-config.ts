/**
 * App Config Transformer for Nuxt Content Studio
 * This transformer is used by the Nuxt Content Studio preview mode
 * to transform the app config before applying it.
 */
export default (config: any) => {
  return config;
};
